import {updateObject} from '../../shared/utilty';

const initialState = {
    chats: [],
    loading: false,
    error: null
}

const reducer = (state, action) => {
    switch (action.type) {

    }
}

export default reducer;

